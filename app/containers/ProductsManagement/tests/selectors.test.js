// import { fromJS } from 'immutable';
// import { makeSelectProductsManagementDomain } from '../selectors';

// const selector = makeSelectProductsManagementDomain();

describe('makeSelectProductsManagementDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
