// import { fromJS } from 'immutable';
// import { makeSelectCheckoutDomain } from '../selectors';

// const selector = makeSelectCheckoutDomain();

describe('makeSelectCheckoutDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
