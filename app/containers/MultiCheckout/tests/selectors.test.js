// import { fromJS } from 'immutable';
// import { makeSelectMultiCheckoutDomain } from '../selectors';

// const selector = makeSelectMultiCheckoutDomain();

describe('makeSelectMultiCheckoutDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
