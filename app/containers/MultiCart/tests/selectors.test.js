// import { fromJS } from 'immutable';
// import { makeSelectMultiCartDomain } from '../selectors';

// const selector = makeSelectMultiCartDomain();

describe('makeSelectMultiCartDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
