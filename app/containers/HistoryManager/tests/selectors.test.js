// import { fromJS } from 'immutable';
// import { makeSelectHistoryManagerDomain } from '../selectors';

// const selector = makeSelectHistoryManagerDomain();

describe('makeSelectHistoryManagerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
