// import React from 'react';
// import { shallow } from 'enzyme';

// import TransactionsTimedList from '../index';

describe('<TransactionsTimedList />', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
